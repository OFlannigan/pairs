name: "Setup environment"
description: "Sets up the environment and installs rust"
runs:
  using: "composite"
  steps:
    - name: Checkout code
      uses: actions/checkout@v6
    - name: Set up Rust
      uses: actions-rust-lang/setup-rust-toolchain@v1
