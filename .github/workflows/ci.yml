name: CI
on:
  - push
  - pull_request
  - workflow_dispatch
jobs:
  test:
    name: Runs tests
    uses: ./.github/workflows/test.yml

  build:
    needs: test
    name: Build project
    uses: ./.github/workflows/build.yml
